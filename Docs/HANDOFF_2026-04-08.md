# RuntimeInspector Handoff 2026-04-08

本文档记录本次会话已经完成的工作、验证证据、当前仓库状态，以及下一位 agent/工程师接手时应遵循的入口。

本文件是 **handoff 记录**，不是长期 authority。  
长期开发规则请看 `Docs/AGENT_DEVELOPMENT.md`。

## 1. 本次会话完成项

### 1.1 UI / UX 主线

本轮已经把 RuntimeInspector 的顶层信息架构稳定到：

- `Actor`
- `Changes`
- `Settings`
- `Tools`

已完成的重点：

- 把旧的 `File / Setting / Test` 语义收敛到当前命名与职责
- 建立共享 `Context Strip`，作为唯一上下文摘要源
- `Changes` 页收成日常主路径
- `Tools` 页收容 workflow / self-test / remote / diagnostics
- 建立 UI guardrails 与对应 self-test 覆盖
- 按钮、输入框、section title 已进入统一 theme token / preset 体系
- `SoftContrast` 与 `StudioSlate` 两套 preset 已落地
- theme preset 已支持设置项和预览链

### 1.2 Packaged Runtime 自动验收链

已完成主工程 `PluginMaker` 下的 packaged runtime validation 闭环：

- `Scripts/BuildPackagedRuntimeValidation.cmd/.ps1`
- `Scripts/RunPackagedRuntimeValidation.cmd/.ps1`
- `Scripts/StopPackagedRuntimeValidation.cmd/.ps1`

已完成效果：

- 可以自动构建 packaged validation build
- 可以自动拉起 loopback packaged runtime
- 可以复用已有健康进程
- 可以从脏状态恢复
- `mainline_full_closure` 不再因为“没有手动开 packaged runtime”而卡死

### 1.3 Fab 展示态与媒体 capture

已完成：

- `fab_screenshot_foundation` workflow
- `Scripts/OpenFabScreenshotState.cmd/.ps1`
- `Scripts/CaptureFabMedia.cmd/.ps1`

当前自动化能力：

- 先进入统一展示态
- 再自动切页并通过 UE MCP 整窗截图抓图
- 默认输出到 `Saved/RuntimeInspector/FabMediaCapture/`

当前已自动生成的图片：

- `cover.png`
- `screenshot_01_actor_panel.png`
- `screenshot_02_changes_workflow.png`
- `screenshot_03_settings.png`
- `screenshot_04_tools.png`
- `screenshot_05_remote_session.png`
- `screenshot_06_promote_or_audit.png`

### 1.4 文档权威源收口

已完成：

- 新增唯一 agent authority：
  - `Docs/AGENT_DEVELOPMENT.md`
- 保留唯一 agent 附录：
  - `Docs/UI_GUARDRAILS.md`
- 其余文档全部降成用户文档、Fab 文档或附录
- 删除整套过期 `Docs/TaskPacks/...`

## 2. 本次会话关键文件

这次会话直接相关、后续最值得先看的文件：

- `Docs/AGENT_DEVELOPMENT.md`
- `Docs/UI_GUARDRAILS.md`
- `Source/RuntimeInspector/Private/InspectorWorldSubsystem.cpp`
- `Source/RuntimeInspector/Private/InspectorRemoteSession.cpp`
- `Scripts/BuildPackagedRuntimeValidation.ps1`
- `Scripts/RunPackagedRuntimeValidation.ps1`
- `Scripts/StopPackagedRuntimeValidation.ps1`
- `Scripts/OpenFabScreenshotState.ps1`
- `Scripts/CaptureFabMedia.ps1`

## 3. 验证证据

### 3.1 构建与发布

- Fab release package log:
  - `Saved/build_runtimeinspector_fab_release.log`

### 3.2 Packaged runtime validation

- packaged validation build logs:
  - `Saved/build_packaged_runtime_validation.log`
  - `Saved/build_packaged_runtime_validation_game.log`

### 3.3 Fab screenshot state / media capture

- screenshot state log:
  - `Saved/open_fab_screenshot_state.log`
- media capture output:
  - `Saved/RuntimeInspector/FabMediaCapture/`
- media capture log:
  - `Saved/RuntimeInspector/FabMediaCapture/capture_fab_media.log`
- media capture manifest:
  - `Saved/RuntimeInspector/FabMediaCapture/capture_manifest.txt`

### 3.4 Runtime self-tests / mainline

本次会话最终确认过的关键结果：

- `ContextStripSelfTest=PASS`
- `FilePageInjectionSelfTest=PASS`
- `WorkflowPageViewSelfTest=PASS`
- `TestPageLayoutSelfTest=PASS`
- `SettingsPageLayoutSelfTest=PASS`
- `ThemePresetPreviewSelfTest=PASS`
- `FileWorkflowSelfTest=PASS`
- `FileRoleCompareViewSelfTest=PASS`
- `FileRemoteSessionCompareViewSelfTest=PASS`
- `mainline_remote_packaged_matrix_default=PASS`
- `mainline_full_closure=PASS | Passed=23 Failed=0`

## 4. 当前仓库状态

需要注意：

- 当前 worktree 不是干净的
- 仓库里仍有较多已有修改和未跟踪文件
- 不要假设自己面对的是“一次会话只改了少数文件”的整洁状态
- 后续 agent 不应直接回滚未理解的改动

当前文档结构状态：

- `Docs/AGENT_DEVELOPMENT.md` 是唯一 authority
- `Docs/UI_GUARDRAILS.md` 是唯一 UI 附录
- `README.md`、`USER_GUIDE_zh-CN.md`、`FAB_*`、`FabMedia/README.md` 都只是非 authority 文档

## 5. 下一位接手时的建议入口

### 5.1 如果要继续做开发

按顺序看：

1. `Docs/AGENT_DEVELOPMENT.md`
2. `Docs/UI_GUARDRAILS.md`
3. `Source/RuntimeInspector/Private/InspectorWorldSubsystem.cpp`

### 5.2 如果要继续做 Fab 上架

按顺序看：

1. `FAB_SUBMISSION_CHECKLIST.md`
2. `FabMedia/README.md`
3. `Saved/RuntimeInspector/FabMediaCapture/`

### 5.3 如果要继续做 packaged runtime

按顺序看：

1. `Scripts/BuildPackagedRuntimeValidation.ps1`
2. `Scripts/RunPackagedRuntimeValidation.ps1`
3. `Scripts/StopPackagedRuntimeValidation.ps1`
4. `Source/RuntimeInspector/Private/InspectorRemoteSession.cpp`

## 6. 建议的下一步

当前最有价值的后续项有三个：

1. 补一条 `demo.gif` 或 `demo.mp4` 的录制链
2. 做一次最终 Fab 提审前人工 smoke：
   - blank host 安装/加载
   - 主工程截图态检查
   - 最终素材筛选与复制到 `FabMedia/`
3. 如有时间，清理 `warning C4828` 这类非阻塞构建噪音

## 7. 本次 handoff 的边界

本文件只记录这次会话的结果，不应该继续演化成第二个 authority 文档。

后续如果开发规则变了：

- 更新 `Docs/AGENT_DEVELOPMENT.md`
- 不要把新的规范只写在 handoff 里
